# MQTT Configuration
MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_USER=
MQTT_PASSWORD=
MQTT_BASE_TOPIC=sensors

# Sensor Configuration
SENSOR_READ_INTERVAL=5

# Note: Update these values as needed for your environment
# MQTT_USER and MQTT_PASSWORD are optional (leave empty for anonymous)
